# ============================================
# SES Manager - Environment Variables
# ============================================
# Copy this file to .env and fill in your values

# ============================================
# Database Configuration (REQUIRED)
# ============================================
# PostgreSQL connection string
# Local setup: Install PostgreSQL and create a database, then use:
DATABASE_URL="postgresql://username:password@localhost:5432/ses_manager"

# ============================================
# Server Configuration
# ============================================
# Environment: development or production
NODE_ENV="development"

# Port for the server (default: 5000)
PORT=5000

# ============================================
# Session & Encryption Configuration (REQUIRED)
# ============================================
# Secret key used for:
#   - Session encryption and management
#   - AWS credential encryption in database
# 
# Generate a strong random string (32+ characters recommended):
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 
# IMPORTANT: Never share this secret or commit it to version control
# For local dev, any string works but use something secure in production
SESSION_SECRET="your-secret-key-here-change-this-in-production"

# ============================================
# AWS Credentials (REQUIRED for email functionality)
# ============================================
# You can set these here OR configure them through the web interface
# AWS SES credentials
AWS_ACCESS_KEY_ID="your-aws-access-key"
AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
AWS_REGION="us-east-1"

# ============================================
# Replit OAuth (Optional - Only for Replit deployment)
# ============================================
# These are automatically set by Replit, leave blank for local development
# REPL_ID=""
# REPLIT_DOMAINS=""
# ISSUER_URL="https://replit.com/oidc"

# ============================================
# Development Settings
# ============================================
# Set to true if running on Replit (automatically detected)
# REPLIT_ENV=true